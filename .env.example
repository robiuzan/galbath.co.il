# ── גל אמבטיות (Gal Ambatyot) environment template ─────────────────────────
# Copy to `.env.local` and fill real values. Never commit `.env.local`.

# Public site URL (used for canonical URLs, sitemap, OG tags)
NEXT_PUBLIC_SITE_URL="https://galbath.co.il"

# Business contact (also mirrored in lib/site-config.ts / site.config.json)
NEXT_PUBLIC_PHONE="055-6601006"
NEXT_PUBLIC_WHATSAPP="972556601006"

# Lead routing — server-side only (public/lead.php)
LEAD_EMAIL_TO="service@galbath.co.il"         # 🔶 confirm send-to address
